# Mode de rendu du hero.
#   keyframes = 3 images clés interpolées (défaut, aucune extraction requise)
#   frames    = séquence extraite des clips fal.ai (rendu final)
NEXT_PUBLIC_HERO_MODE=keyframes
